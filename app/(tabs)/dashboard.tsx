import React from 'react';
// Import giao diện quản lý đã tách trước đó
import ProviderDashboard from '../../components/ui/ProviderDashboard';

export default function DashboardScreen() {
  return <ProviderDashboard />;
}